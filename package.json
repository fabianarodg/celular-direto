{
  "name": "celular-direto",
  "private": true,
  "version": "1.0.0",
  "description": "Teste para a vaga de Front-End na Wooza",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.7.7",
    "gulp-compass": "^2.1.0",
    "gulp-concat": "^2.6.1",
    "gulp-minify-css": "^1.2.4",
    "run-sequence": "^2.2.1"
  },
  "scripts": {
    "app": "gulp && http-server -a localhost -p 8000",
    "start": "npm install && bower install && npm run app"
  },
  "dependencies": {
    "gulp": "^3.9.1"
  }
}
